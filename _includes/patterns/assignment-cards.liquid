<section class="pad-t-4 pad-b-4 bg-grey-light" tabindex="0">
  <div class="container-grid">
    {% include snippets/heading-linked.liquid text="Assignments" class="shift-full" %}
    {% for deliverable in site.assignments %}
      {% if deliverable.active %}
        {% include patterns/cards/link-deliverable.liquid class="l-span-6" %}
      {% endif %}
    {% endfor %}
  </div>
</section>